/**
 * Remediator metadata key for date.
 * @type {string}
 */
export const LIB_DATE_KEY = 'date';

/**
 * Remediator metadata key for encountered error if skipErrors is enabled.
 * @type {string}
 */
export const LIB_ERROR_KEY = 'error';

/**
 * Remediator metadata key for camera device make.
 * @type {string}
 */
export const LIB_MAKE_KEY = 'deviceMake';

/**
 * Remediator metadata key for camera device model.
 * @type {string}
 */
export const LIB_MODEL_KEY = 'deviceModel';

/**
 * Remediator metadata key for file orientation.
 * @type {string}
 */
export const LIB_ORIENTATION_KEY = 'orientation';

/**
 * Remediator metadata key for original file path.
 * @type {string}
 */
export const LIB_ORIGINAL_FILE_PATH = 'filePath';

/**
 * Remediator metadata key for file pixel resolution.
 * @type {string}
 */
export const LIB_RESOLUTION_KEY = 'resolution';
